package ch.admin.foitt.wallet.platform.locale

/**
 * Debug-locale for translators: every string value equals its key.
 *
 * The resource (values-zz/strings.xml) is generated at build time for specified flavors only and
 * gated at runtime by BuildConfig.DEBUG_LOCALE_ENABLED.
 */
object DebugLocale {
    const val LANGUAGE = "zz"
    const val LABEL = "Text keys"
}
